$(window).on("resize", function (e) {
    if(window.innerWidth >= 1085){
        window.location.href = $(".link_type_config").attr("href");
    }
});

if(window.innerWidth >= 1085){
    window.location.href = $(".link_type_config").attr("href");
}

